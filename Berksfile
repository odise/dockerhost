source "https://supermarket.getchef.com"

cookbook 'docker'
cookbook "systemd", github: "odise/systemd-cookbook"
cookbook "sudo"

#Dir.glob('./cookbooks/*').each do |path|
#  cookbook File.basename(path), path: path if File.ftype(path).eql?('directory')
#end
